# 《问窟 GrottoMind》设计与交互规范说明书（2.0 升级版）

> **版本**：2.0.0 (深度重构与性能升级版)  
> **定位**：南京栖霞山石窟造像 AI 数字复彩与学术文献交互档案馆  
> **核心气质**：沉静、庄重、克制、东方空间感、博物馆沉浸展、数字化学术严谨性

---

## 1. 核心视觉哲学与定位

《问窟 GrottoMind》不是普通文旅宣传页，也不是功能堆砌的 AI 工具箱，而是一座具有**电影级空间叙事与数字化考古严谨度**的沉浸式文化遗产交互档案馆。

### 1.1 七大视觉铁律
1. **沉静克制**：色彩与动效必须服务于历史与文物本身，绝无浮夸的炫技与廉价古风。
2. **绝对禁止使用 Emoji 与廉价低质图标**：全站坚决禁止出现任何 Emoji 表情符号（如 📜/🖼️/📑/🤖/✨/🔥/💡/📌 等）或粗糙厚重的第三方彩标。所有图形统一采用**极简东方金石微线框（1px 细线 SVG 图标，金属微光描边）或纯正中文字标/篆刻印记**，确保国家级学术馆藏的高级质感。
3. **场景先行**：以 3D 舍利塔、石窟岩壁、微观风化痕迹为第一视觉语言，UI 界面降至最低干扰。
4. **东方审美与现代技术融合**：使用传统矿物颜料色谱（赭石、朱砂、石青、石绿、泥金），配合现代精准的排版系统。
5. **数字化学术严谨性**：严格区分“历史留存现状”与“AI 推演复彩”，所有复彩推演必须带有学术边界说明。
6. **极简 UI 与空间留白**：坚决摒弃常规 Dashboard 网格、大面积粗暴毛玻璃、霓虹描边与图标堆叠。
7. **全终端无缝沉浸与极速性能**：视觉表现与 Lighthouse 95+ 性能指标深度协同，视觉不以牺牲加载速度为代价。

---

## 2. 工程化 Design Tokens 规范（CSS 变量标准）

所有前端样式必须基于以下统一的 CSS Custom Properties，禁止在业务组件内硬编码 ad-hoc 颜色与尺寸。

```css
:root {
  /* ==================== 1. 空间与岩壁底色 ==================== */
  --bg-deep-rock: #141413;        /* 极深岩壁背景 (主底色) */
  --bg-cave-void: #1c1c1a;        /* 石窟幽暗空间 */
  --bg-surface-elevated: #242422;  /* 浮层、展签卡片背景 */
  --bg-veil-mask: rgba(14, 14, 13, 0.88); /* 全局黑幕与转场遮罩 */

  /* ==================== 2. 传统矿物色系 (复彩与高光) ==================== */
  /* 泥金 (佛光、细边框、进度微光) */
  --gold-primary: #f6cea0;
  --gold-line: #d4a96a;
  --gold-dim: rgba(212, 169, 106, 0.25);
  --gold-glow: rgba(246, 206, 160, 0.15);

  /* 朱砂 (暖调复彩、印章、核心焦点) */
  --cinnabar-main: #c9372c;
  --cinnabar-shadow: #8b2500;
  --cinnabar-soft: rgba(201, 55, 44, 0.18);

  /* 石青与石绿 (冷调矿物、壁画底色、历史沉淀) */
  --azurite-blue: #2e5c8a;        /* 石青 (蓝铜矿) */
  --malachite-green: #4a7c59;     /* 石绿 (孔雀石) */
  --mineral-cyan: #3b6e7a;

  /* 丹枫与赭石 (栖霞地域特色、风化岩质) */
  --ochre-rock: #7d4e38;          /* 赭石 */
  --maple-autumn: #c44d34;        /* 丹枫 */

  /* ==================== 3. 文字与前景色阶 ==================== */
  --text-primary: rgba(255, 255, 255, 0.94);   /* 主文案、大标题 */
  --text-secondary: rgba(255, 255, 255, 0.72); /* 展签说明、正文 */
  --text-muted: rgba(255, 255, 255, 0.45);     /* 次要元数据、文献出处 */
  --text-dark-contrast: #151412;              /* 按钮反色状态下的深墨字 */

  /* ==================== 4. 排版与字阶系统 ==================== */
  --font-display: 'ZiXinFangMengHuanGeTeTi', 'Noto Serif SC', serif;
  --font-serif: 'Noto Serif SC', 'Source Han Serif SC', serif;
  --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
  --font-num: 'New Rocker', 'Cormorant Garamond', serif;

  --text-xs: 0.75rem;    /* 12px 辅助注记、标本编号 */
  --text-sm: 0.875rem;   /* 14px 正文次级、按钮文字 */
  --text-base: 1rem;     /* 16px 标准展签正文 */
  --text-lg: 1.125rem;   /* 18px 章节副标 */
  --text-xl: 1.5rem;     /* 24px 模块标题 */
  --text-2xl: 2.25rem;   /* 36px 序章字幕 */
  --text-hero: 4rem;     /* 64px 最终品牌视觉字 */

  /* ==================== 5. 动效与缓动标准 ==================== */
  --ease-cinematic: cubic-bezier(0.25, 1, 0.5, 1);    /* 展品呼吸、运镜淡出 */
  --ease-smooth: cubic-bezier(0.16, 1, 0.3, 1);       /* 悬浮微交互 */
  --duration-fade: 0.8s;
  --duration-interaction: 0.3s;
}
```

---

## 3. 字体与排版规范（Lighthouse 95+ 优化）

### 3.1 字体加载与分层
为确保首屏 FCP / LCP 达到极致（< 1.2s），坚决废弃外部 Google Fonts 同步阻塞外链：

1. **核心大字与叙事字体 (`ZiXinFangMengHuanGeTeTi`)**：
   - 提取包含项目序章、章节大标题的 **轻量化子集**（体积控制在 80KB 以内），放置于 `/public/assets/`。
   - 使用 `@font-face` 声明并配置 `font-display: swap`。
2. **正文字体 (`Noto Serif SC`)**：
   - 优先使用本地已安装的系统思源宋体/苹方宋体，回退至经过字符子集化的 Web 字体。
3. **数字与编号 (`New Rocker`)**：
   - 仅包含 `0-9`、`%`、`.`、`-` 等字符集（< 15KB）。

### 3.2 排版禁忌
- ❌ **严禁**大段正文（> 3行）采用居中对齐；展签与学术文献必须采用左对齐并保留合理行距（`line-height: 1.7`）。
- ❌ **严禁**同时出现多个视觉焦点或两句以上全屏大字幕。
- ❌ **严禁**在文物说明中使用任何夸张感叹号。

---

## 4. 章节页面与核心组件交互设计规范

### 4.1 序章：四段式仪式感滚动
- **视觉层级**：背景视频（1080p WebM/MP4） → 雾气与浮尘 Shader → 暗色遮罩 → 中央大字字幕 → 右下角圆形控制。
- **状态流转**：每次用户滚轮触发明确的叙事步进，上一句字幕完全淡出后，下一句方可淡入（禁止重叠混杂）。

---

### 4.2 第一章：时间轴与 3D 舍利塔交互（TimelineHall）
- **3D 视点联动**：
  - 相机停靠点（Stops）平滑过渡（GSAP `power2.inOut` 缓动，1.2s 时长）。
  - 视点聚焦时（如塔刹、须弥座释迦八相成道图、天王像），对应佛龛热点微光脉冲出现。
- **性能规范**：
  - 3D 模型采用 Draco 几何压缩 + WebP/KTX2 纹理，资源在后台异步拉取，未加载完成前以高质感线框剪影骨架平滑占位。
  - 相机停止运动后自动进入按需渲染模式，暂停无意义的 GPU 空转。

---

### 4.3 第二章：风化与褪色解构展厅（FadingHall 重构）
重构原先混乱的长卡片堆叠，采用**双层画卷拉链（Curtain Slider）与显微探照**交互：

1. **双层画卷拉链对比**：
   - 左侧为“现状：千年风化残存岩壁”，右侧为“推演：AI 数字复彩显影”。
   - 用户可自由左右拖动中轴金线拉链，直观观察颜料剥蚀与复原边界。
2. **矿物成分显微浮窗（Micro-Mineral View）**：
   - 点击浮雕区域（如菩萨天衣），弹出极简微观展签，展示 XRF/XRD 光谱数据及矿物成分（如“天然朱砂：HgS 含量 96.2%”）。
3. **色彩显影动效**：
   - 复彩色从岩壁暗部由深至浅如水墨晕染扩散，伴随极微弱的沙沙声（水墨浸润音效）。

---

### 4.4 第三章：共创工坊与文献馆（GrottoHub 重构）
1. **共创色彩卡片**：
   - 摒弃常规表单，采用“意象取色盘”交互（用户选取“金陵晨雾”、“摄山丹枫”、“六朝烟水”等意象）。
   - 生成具有传统古籍笺纸质感的高保真复彩卡片，右下角带有朱砂印章标识（“数字复彩推演”）。
   - 支持一键导出高分辨率 PNG 画卷。
2. **学术文献图书馆（LiteratureLibrary）**：
   - 采用古典经卷式双栏排版：左侧为学术文献索引树（朝代/石窟编号/颜料分析分类），右侧为阅读视窗。
   - 关键词与石窟造像具备双向锚点高亮。

---

### 4.5 全局智能体助手（GlobalAgent 升级）
1. **面板形态**：
   - 右侧滑出的半透明东方简牍式侧边栏（宽度 420px，移动端全屏）。
   - 背景使用 `--bg-surface-elevated` 并带有 1px 细金线边缘。
2. **DeepSeek-R1 思考链（Reasoning Process）展示规范**：
   - 思考过程采用手风琴折叠式呈现，标注微光“考古推演中... / 推演脉络”。
   - 内部以淡金小字展示逻辑推导，不喧宾夺主。
3. **学术引用卡片联动（Citation Badges）**：
   - AI 回答中的学术出处以可交互的角标呈现（如 `[文献: 舍利塔造像研究 §3]`）。
   - 点击角标直接在左侧弹出对应文献段落的引文卡片，绝非虚空编造。

---

## 5. 无障碍与体验保障（WCAG 2.1 AA 级规范）

为实现 Lighthouse Accessibility 95+ 分，全站必须遵守以下硬性无障碍要求：

1. **色彩对比度**：
   - 所有 `--text-primary` 对 `--bg-deep-rock` 背景的对比度不得低于 `12:1`（远超 AA 级的 4.5:1）。
   - 次级文字 `--text-secondary` 对比度必须维持在 `5.5:1` 以上。
2. **键盘导航与焦点态 (`:focus-visible`)**：
   - 所有按钮、控制开关、时间轴节点均支持 `Tab` 键聚焦。
   - 聚焦状态显示 `2px solid var(--gold-line)`，带有 `3px` 外轮廓偏移，严禁使用浏览器原生杂色轮廓。
3. **屏幕阅读器语义声明**：
   - 所有图标按钮（全屏、音频、AI 触发器）必须显式具备精准的 `aria-label`。
   - 3D Canvas 容器声明 `role="region"` 与 `aria-label="栖霞山舍利塔三维造像模型互动区"`。

---

## 6. 音效与触觉反馈规范

1. **默认静音**：全站音频默认关闭，由用户在序章或控制栏主动触发开启。
2. **石质空间混响（Web Audio Reverb）**：
   - 按钮 Hover：极轻微石面轻触感（< 0.08s，音量 5%）。
   - 章节跳转：石窟空间气流流动低频音（0.5s 淡入淡出，音量 10%）。
   - 复彩显影：颜料在宣纸上晕开的沙沙音（1.5s，音量 12%）。
3. **绝对红线**：
   - ❌ 严禁出现循环轰炸的流行背景音乐。
   - ❌ 严禁出现尖锐的电子蜂鸣音。

---

## 7. 视觉验收清单（Checklist）

每次提交前端代码前，必须进行以下自检：
- [ ] 是否在首屏或主界面引入了任何未在 Tokens 中定义的硬编码色值？
- [ ] 是否存在长篇大论、过度热情的宣传型文案？
- [ ] 3D 模型和重型动效在静止时是否已停止无意义的重绘？
- [ ] 所有交互元素是否具备规范的 hover / active / focus-visible 状态？
- [ ] 复彩推演区域是否均带有严谨的“数字复彩推演”学术说明标识？
